import { WebmContainer } from "./WebmContainer";
export declare class WebmFile extends WebmContainer {
    constructor(source: Uint8Array);
    getType(): string;
    toBlob(mimeType?: string): Blob;
    static blobToArray(blob: Blob): Promise<Uint8Array>;
    static fromBlob(blob: Blob): Promise<WebmFile>;
}
