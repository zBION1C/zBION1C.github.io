// Sortedset iteration
foreach (string elemento in sortedSet)
{
    // Esegui operazioni con l'elemento
}

// Add
public bool Add (T item);

// Remove
public bool Remove (T item);

// There is no set method

// There is no get method

// Find
public virtual bool Contains (T item);